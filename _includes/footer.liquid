{% comment %}
  Local override of al_folio_core's footer.liquid.

  Upstream renders a fixed bar with "© Copyright <year> <name>. Powered by
  Jekyll and al-folio." This site does without it, so nothing is emitted here.
  al-folio is MIT-licensed, so the credit is a courtesy rather than a
  requirement; restore it by deleting this file.
{% endcomment %}
